extend /partials/layout

block append config
  - page.title = "Australia’s Key Heads of Mission"
  - var chart  = data.getOne("static/charts/australian-heads-of-mission/data")

block page
  script
    != `window.chart = ${JSON.stringify(chart)};`
  .wrapper(data-view="australian-heads-of-mission" data-source="chart")
    svg.chart(width="100%" height=360)
